# Versões PRESAS no que foi testado.
#
# ⚠️ Sem `==`, o servidor instala o que estiver mais novo no dia do deploy — que
# não é o que passou nas 811 verificações aqui. Uma quebra assim aparece no
# start, em produção, e o log fala de uma biblioteca que ninguém tocou.
#
# Para subir uma versão: mude aqui, rode a bateria inteira na base local, e só
# então promova para `producao`.

fastapi==0.115.0
uvicorn[standard]==0.30.6
psycopg2-binary==2.9.10
pydantic[email]==2.11.7
python-dotenv==1.0.1
PyJWT==2.8.0
bcrypt==4.0.1
python-multipart==0.0.9
Pillow==12.2.0
httpx==0.28.1
cryptography==45.0.7
reportlab==5.0.1
# ⚠️ **O banco de fusos, e ele NÃO vem no contêiner.** `zoneinfo` lê a base do
# sistema operacional, e a imagem enxuta do deploy não a traz: sem este pacote,
# `ZoneInfo("America/Sao_Paulo")` levanta `ZoneInfoNotFoundError` — em produção,
# no start, falando de um fuso que ninguém tocou. Em Windows ele é obrigatório
# sempre, porque lá essa base nunca existe.
tzdata==2025.2
